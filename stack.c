#include "monty.h"

/**
 * f_stack - Changes the order to stack mode (LIFO).
 * @head: Memory allocation head of the stack
 * @counter: Line number in the Monty file
 * Return: Always 0
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
