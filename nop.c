#include "monty.h"

/**
 * f_nop - Does nothing (No operation).
 * @head: Pointer to the head of the stack
 * @counter: Line number in the Monty file
 * Return: No return
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
}
